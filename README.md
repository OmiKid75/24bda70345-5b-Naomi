# Student Management System

Scaffolded from the project guide. Run `pnpm install` (or `npm install`) then set `MONGO_URI` in `.env` and start the app with `pnpm dev`.

- API endpoints: `/students`
- UI: `http://localhost:3000/view/students`
